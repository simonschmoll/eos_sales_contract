{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "cd tests && npm install && cd ../frontend && npm install",
    "start": "cd frontend && npm start",
    "test": "cd tests && npm test"
  }
}
